- content_for :header_title do
  Edit server #{@server.name}
- content_for :header_actions do
  = link_to server_path(@server), class: 'btn btn-primary' do
    %span.fa.fa-arrow-left
    Back

= render 'form'
